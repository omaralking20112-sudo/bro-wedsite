<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NED - RED ZONE</title>
    <style>
        body {
            background-color: #000;
            color: #ff0000;
            font-family: 'Courier New', Courier, monospace;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            overflow: hidden;
        }

        /* المطر الحقيقي */
        #canvas-rain {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        .container {
            width: 85%;
            max-width: 340px;
            border: 2px solid #ff0000;
            border-radius: 35px; 
            padding: 40px 25px;
            text-align: center;
            background: rgba(0, 0, 0, 0.85);
            backdrop-filter: blur(12px);
            position: relative;
            z-index: 1;
            box-shadow: 0 0 30px #ff0000, inset 0 0 20px #500;
            animation: pulse-glow 2.5s infinite alternate;
        }

        @keyframes pulse-glow {
            from { box-shadow: 0 0 20px #ff0000, inset 0 0 10px #700; }
            to { box-shadow: 0 0 40px #ff0000, inset 0 0 25px #a00; }
        }

        .glitch {
            font-size: 3rem;
            font-weight: bold;
            letter-spacing: 10px;
            margin-bottom: 5px;
            color: #ff0000;
            text-shadow: 0 0 15px #ff0000;
            animation: glitch-effect 0.6s infinite;
        }

        @keyframes glitch-effect {
            0% { transform: translate(0); text-shadow: 2px 2px #ff0000; }
            25% { transform: translate(-2px, 2px); }
            50% { transform: translate(2px, -2px); text-shadow: -2px -2px #500; }
            100% { transform: translate(0); }
        }

        .separator {
            margin: 15px auto;
            width: 80%;
            height: 1px;
            background: linear-gradient(90deg, transparent, #ff0000, transparent);
            opacity: 0.6;
        }

        .info-text {
            color: #fff;
            font-size: 0.85rem;
            margin: 15px 0;
            line-height: 1.8;
            text-align: left;
            padding: 0 10px;
        }

        .highlight { 
            color: #ff0000; 
            font-weight: bold; 
            text-transform: uppercase;
        }

        /* قسم الدفع */
        .payment-methods {
            margin-top: 10px;
            text-align: left;
            padding: 0 10px;
        }

        .payment-item {
            font-size: 0.8rem;
            color: #ccc;
            display: flex;
            align-items: center;
            margin-bottom: 5px;
        }

        .payment-item::before {
            content: "◆";
            color: #ff0000;
            margin-right: 8px;
            font-size: 0.7rem;
        }

        .btn {
            display: block;
            width: 100%;
            padding: 16px 0;
            margin: 25px 0 10px 0;
            background: rgba(255, 0, 0, 0.05);
            color: #ff0000;
            border: 2px solid #ff0000;
            border-radius: 15px;
            text-decoration: none;
            font-weight: bold;
            font-size: 1rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            transition: all 0.4s ease;
            box-sizing: border-box;
        }

        .btn:hover {
            background: #ff0000;
            color: #000;
            box-shadow: 0 0 40px #ff0000;
            transform: translateY(-3px);
        }

        .terminal {
            text-align: left;
            font-size: 0.7rem;
            margin-top: 20px;
            color: #666;
            border-top: 1px solid rgba(255, 0, 0, 0.1);
            padding-top: 15px;
        }
    .success { color: #00ff00; }
        .blink { animation: blinker 1.2s linear infinite; }
        @keyframes blinker { 50% { opacity: 0; } }
    </style>
</head>
<body>

<canvas id="canvas-rain"></canvas>

<div class="container">
    <div class="glitch">NED</div>
    <div class="separator"></div>
    
    <div class="info-text">
        <div><span class="highlight">Identity:</span> @py3_3</div>
        <div><span class="highlight">Location:</span> Iraq - Basra</div>
        <div><span class="highlight">Status:</span> Secure Connection</div>
    </div>

    <div class="separator"></div>

    <div class="payment-methods">
        <span class="highlight" style="font-size: 0.8rem; display: block; margin-bottom: 8px;">Accepted Payments:</span>
        <div class="payment-item">Asiacell (Voucher/Transfer)</div>
        <div class="payment-item">MasterCard / Visa</div>
    </div>

    <a href="https://t.me/@py3_3" target="_blank" class="btn">Telegram Profile</a>

    <div class="terminal">
        [!] SYSTEM: <span class="success">ENCRYPTED</span><br>
        [!] USER: <span style="color:#fff;">NED</span><br>
        <span class="blink" style="color:#ff0000;">> READY TO PROCEED_</span>
    </div>
</div>

<script>
    const canvas = document.getElementById('canvas-rain');
    const ctx = canvas.getContext('2d');
    let width, height, drops;

    function init() {
        width = window.innerWidth;
        height = window.innerHeight;
        canvas.width = width;
        canvas.height = height;
        drops = [];
        for (let i = 0; i < width / 2.5; i++) {
            drops.push({
                x: Math.random() * width,
                y: Math.random() * height,
                v: Math.random() * 15 + 10, 
                l: Math.random() * 25 + 10
            });
        }
    }

    function draw() {
        ctx.fillStyle = 'rgba(0, 0, 0, 0.2)'; 
        ctx.fillRect(0, 0, width, height);
        ctx.strokeStyle = 'rgba(255, 0, 0, 0.6)';
        ctx.lineWidth = 1.2;
        ctx.lineCap = 'round';
        drops.forEach(d => {
            ctx.beginPath();
            ctx.moveTo(d.x, d.y);
            ctx.lineTo(d.x, d.y + d.l);
            ctx.stroke();
            d.y += d.v;
            if (d.y > height) {
                d.y = -d.l;
                d.x = Math.random() * width;
            }
        });
        requestAnimationFrame(draw);
    }

    window.addEventListener('resize', init);
    init();
    draw();
</script>

</body>
</html>
